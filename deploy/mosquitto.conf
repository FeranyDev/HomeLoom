# Development-only broker. Production deployments must enable authentication,
# ACLs and TLS instead of anonymous access.
listener 1883 0.0.0.0
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
